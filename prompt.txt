帮我在Flash-Sim项目中完成以下任务：
1. 在./precondition/pre_trace.json文件中生成一个用于初始条件生成的trace，指令结构与parser中的COMMAND_SCHEMA相同，但是只包含write, read, static_write三种指令
2. 在engine中写好precondition函数，读取pre_trace_path路径对应的文件，调用parser完成解析，并压入event_queue，scheduled_time均为0时刻
3. 在engine中加入一个pre_sim属性，bool，在调用precondition函数完成pre_trace解析之后将其赋值为true，并调用Start_simulation()开始仿真
4. 所有execute()函数中加入一个判断，当pre_sim为真时，登记事件的scheduled_time均为0
5. 