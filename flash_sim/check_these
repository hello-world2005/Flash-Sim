目前要跑通流程还需要完善的代码：
1. parser将trace文件生成req压入初始的event_queue
2. 正向的操作都实现完了，逆向的流程需要再完善一下，包括：
    - PHY收到flash chip读写完成之后，往回发数据
    - PHY收到数据回传完成之后，往回发req完成信号，发给HIL，HIL再发给host，host收到之后将cq更新，完成仿真
    - 目前TSU Schedule只会在address mapping之后才会触发，但是每次触发不会清空queues，因此必须有其他机制能够让所有提交的transactions最终
    能够被调度执行


3. PHY.send_command_to_chip函数中，没有管SEARCH和COMPUTE的命令，需要补充
4. HIL.fetch_data函数需要实现，目前是占位
5. 目前sq, cq和host memory的交互只是在event queue中插了个事件，没有管具体的内容是不是正确（感觉双边的指针怎么管理很容易乱啊）
6. HIL调用PCIE接口发回结果的时候应该是以input_stream为单位操作的，每个input_stream正在回传数据的时候标记为busy，当所有stream都busy的
时候意味着PCIe接口的并发数已经饱和，需要等待。当stream完成回传之后，标记为idle，可以继续回传下一个message。